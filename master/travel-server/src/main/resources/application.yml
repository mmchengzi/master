spring:
  profiles:
    active: dev
  application:
    name: travelserver

server:
  port: 8085

eureka:
  instance:
      leaseRenewalIntervalInSeconds: 10
      health-check-url-path: /actuator/health
      prefer-ip-address: true
  client:
    registryFetchIntervalSeconds: 5
    serviceUrl:
      defaultZone: http://123.207.98.252:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS

pagehelper:
  helper-dialect: mysql
  reasonable: true
  support-methods-arguments: true
  params: count=countSql

sms:
  BASE_URL: https://api.miaodiyun.com
  ACCOUNT_SID: af1f7455dd5c430585ada20ab693743d
  AUTH_TOKEN: c20566eb1c31421183ab093b76d05209
  RESP_DATA_TYPE: json